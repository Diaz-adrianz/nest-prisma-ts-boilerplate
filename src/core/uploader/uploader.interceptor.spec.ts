import { UploaderInterceptor } from './uploader.interceptor';

describe('UploaderInterceptor', () => {
  it('should be defined', () => {
    expect(new UploaderInterceptor()).toBeDefined();
  });
});
